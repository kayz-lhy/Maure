# Maure AI 开发指南

## 项目定位

轻量级 Go 搜索引擎，适合学习、研究。

## 核心原则

1. **简单优先**：先实现，再优化
2. **代码清晰**：易于理解，方便学习
3. **依赖最少**：尽量使用标准库
4. **注释适度**：公开 API 注释 + 关键逻辑注释

## 开发优先级

**P0**（MVP 必须）：
- [x] 文档添加 ✅
- [x] 分词器 ✅
- [x] 倒排索引 ✅
- [x] 词项查询 ✅
- [x] 评分排序 ✅
- [x] 布尔查询 ✅
- [ ] 短语查询
- [ ] 持久化存储

**P1**（应该要有）：
- [x] 文档删除/更新 ✅ (增量2)
- [ ] 范围查询
- [ ] 模糊查询

**P2**（期望有）：
- [ ] 通配符查询
- [ ] 段合并
- [ ] CLI 完善

## 已完成增量

详见 [docs/INCREMENT.md](docs/INCREMENT.md)

| 增量 | 内容 | 状态 |
|------|------|------|
| 增量 1 | 文档结构与分词器 | ✅ |
| 增量 2 | 倒排索引与内存存储 | ✅ |
| 增量 3 | 评分排序 | ✅ |
| 增量 4 | 布尔查询 | ✅ |
| 增量 5 | 短语查询 | 待开发 |

## 包结构

```
pkg/
├── analyzer/      # 分词器 ✅
├── document/     # 文档结构 ✅
├── index/        # 索引核心 ✅
├── query/        # 布尔查询 ✅
├── search/       # 评分 ✅
└── store/        # 存储接口 ✅
```

## 语言风格

- 文档：中文为主，关键术语英文
- 代码注释：关键逻辑注释
- 变量命名：清晰表达意图

## 测试策略

- 集成测试为主
- 覆盖率 > 50%
- 简单基准测试
- **演示测试**：放在 `test/` 目录，以 `TestDemo_` 开头

## 性能目标

- 10万文档 < 30秒
- 查询延迟 < 100ms
- 内存 < 500MB

## 代码规范

### 包结构

```
pkg/
├── analyzer/      # 分词器
├── document/      # 文档
├── index/         # 索引核心
├── query/         # 查询（待开发）
├── search/        # 评分（待开发）
└── store/         # 存储
```

### 接口设计原则

1. 公开接口以 `er`/`or` 结尾
2. 接口方法不超过 5 个
3. 预留扩展点（如 Analyzer、Directory）

## 禁止事项

- 不要添加不必要的依赖
- 不要过度设计
- 不要跳过测试
- 不要忘记写文档
